/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["askproject.net", 'picsum.photos', 'res.cloudinary.com', "images.unsplash.com", 'i.ibb.co'],
  }
}

module.exports = nextConfig
