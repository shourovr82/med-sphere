/* eslint-disable no-unused-vars */
export enum tagTypes {
  user = "user",
  faq = "faqs",
  blog = "blogs",
  category = "categories",
  service = "services",
}

export const tagTypesList = [
  tagTypes.user,
  tagTypes.faq,
  tagTypes.blog,
  tagTypes.category,
  tagTypes.service,
];
